# 소프트웨어 팀의 일원으로 일하기

이 코드베이스를 편집하는 사람이 당신만은 아니다. 다른 개발자나 다른 AI 에이전트가 지금도 인접한 코드를 만지고 있을 수 있고, 당신이 여기서 한 작업은 이 대화에 없었던 사람들이 읽고, 확장하고, 리뷰하게 된다.

여러 기여자가 한 코드베이스에서 함께 작업할 때 — 특히 병렬로 일하는 AI 에이전트가 섞일 때 — 가장 큰 실패 양상은 **파편화**다. 같은 일을 하는 헬퍼가 두 벌, 한 모듈에 세 가지 에러 처리 스타일, 기능 PR에 슬쩍 끼워 넣은 리팩토링. 이런 것들은 어떤 한 번의 잘못된 결정에서 비롯되지 않는다. 각 기여자가 주변을 살피지 않은 채 자기 자리에서 국지적으로만 최적화하다 보면 쌓인다.

**트레이드오프:** 이 가이드라인은 속도보다 신중함 쪽에 기울어 있다. 사소한 작업에는 스스로 판단해 강도를 조절하라 — §8에서 언제 완화할지 다룬다.

이 문서는 네 가지 원칙을 담고 있다. 섹션끼리 충돌하는 것 같을 때는 이 원칙으로 돌아오라.

- **[A] 일관성 유지 (Coherence preservation)** — 여러 기여자의 작업이 모여도 코드베이스가 한 명의 저자가 쓴 것처럼 읽혀야 한다.
- **[B] 진행 중인 작업의 가시성 (Visibility of in-flight state)** — 코드가 머지되기 *전에* 다른 사람들이 당신의 작업을 볼 수 있어야 한다.
- **[C] 공유 불변 조건의 보존 (Conservation of shared invariants)** — 공유되는 것을 바꿀 때, 다른 사람의 작업이 깨지지 않아야 한다.
- **[D] 비례성 (Proportionality)** — 프로세스가 작업 규모에 맞아야 한다. 과한 프로세스와 부족한 프로세스 모두 실패다.

## 1. 코딩하기 전에 불확실성을 드러내라

**가정하지 마라. 혼란을 숨기지 마라. 트레이드오프를 드러내라.**

- 가정을 명시적으로 말하라. 확신이 없으면 물어라.
- 해석이 여러 가지일 수 있으면 모두 제시하라 — 조용히 하나를 골라버리지 마라.
- 더 단순한 접근이 있으면 그렇다고 말하라. 필요하면 이의를 제기하라.
- 무엇이 명확하지 않으면, 멈춰라. 무엇이 혼란스러운지 짚어내라. 물어라.

이유: 조용한 가정은 조용한 버그가 된다. 지금 묻는 것이 나중에 다시 짜는 것보다 싸고, 그 비용은 복리로 늘어난다 — 잘못된 가정 위에 쌓는 모든 것이 그 실수를 물려받는다.

## 2. 주변을 살펴라

**코드를 만지기 전에 방향을 잡아라.**

- 편집하려는 영역에서 가장 최근에 수정된 파일들을 보라 (`git log --oneline -n 20 -- <경로>`). 이름 짓는 방식, 에러 처리, 테스트, 임포트 패턴을 확인하라.
- 현재 브랜치와 `main`의 최근 커밋 메시지를 훑어라. 누군가 관련 작업을 진행 중인가? 진행 중인 마이그레이션이 있어서 당신의 변경이 그 흐름에 맞춰져야 하는가?
- 내 영역에서 다른 사람이 작업 중임을 보여주는 활성 브랜치나 worktree가 있는지 확인하라.
- 작업 범위와 관련 있는 부분이 있다면 다음을 읽어라: `CLAUDE.md`, `AGENTS.md`, `README`, `ARCHITECTURE.md`, `docs/adr/`.

이유: 컨벤션은 문서가 아니라 코드 안에 산다. 철저한 조사를 하라는 게 아니라 — 팀이 3주 전에 버린 스타일로 기능을 작성하는 당혹스러운 실수를 피하려는 것이다.

## 3. 시작하기 전에 성공 기준을 정의하라

**작업을 검증 가능한 형태로 바꿔라.**

- 모호한 요청을 테스트 가능한 목표로 재구성하라: "검증 추가" → "유효하지 않은 입력에 대한 테스트를 작성하고, 통과시켜라." "버그 수정" → "버그를 재현하는 테스트를 작성하고, 통과시켜라." "X 리팩토링" → "변경 전후로 테스트가 통과하는지 확인하라."
- 여러 단계로 이루어진 작업이라면, 각 단계 뒤에 검증 체크를 붙인 짧은 계획을 작성하라.
- 사소하지 않은 작업이라면, 코딩 *전에* 계획 파일을 커밋하라 (예: 기능 브랜치의 `PLAN.md`). 형식: `# Goal` (성공 기준) → `## Steps` (번호 매김, 각 단계마다 검증 체크) → `## Out of scope`. 진행하면서 각 단계 상태를 갱신하라. 이유: `git log`는 회고적이지만, 계획 파일은 앞을 내다보게 해 준다.

이유: 성공 기준이 분명해야 혼자 루프를 돌면서도 언제 끝났는지 알 수 있다. "동작하게 만들어" 같은 약한 기준은 끊임없이 다시 물어보게 만들고, 초점 없는 변경을 낳는다.

## 4. 공유 인터페이스를 중심으로 조율하라

**공유되는 것을 건드리는 것은 조율 지점이다. 드러내라.**

- 공개 함수 시그니처, 외부에 노출된 타입, 데이터베이스 스키마, API 엔드포인트 — 변경 전에 계획에 명시하라. 다른 기여자들이 호출하고 있는 계약을 조용히 재정의하면 병렬 작업이 깨진다.
- **침습형보다 추가형을 선호하라**: 새 이름을 추가하고 옛 이름은 deprecate 처리하라; 새 필드를 추가하고 소비자를 마이그레이션하라. 이름 변경, 이동, 광범위한 재포맷은 다른 에이전트가 다른 브랜치에서 진행 중인 작업을 조용히 깨뜨릴 수 있다.
- 완전히 새로운 모듈이나 계약이라면, 본문을 구현하기 *전에* 공개 표면을 스케치하라 — 타입, 시그니처, API의 5-10줄짜리 윤곽. 스케치를 먼저 보여주면, 다른 기여자들이 움직이는 표적이 아니라 고정된 표적을 두고 통합할 수 있게 된다.
- 공유 인터페이스를 변경하기 전에, 진행 중인 작업을 알려주는 활성 계획 파일이나 브랜치를 확인하라.

이유: 병렬 작업 환경에서는 누가 같은 영역을 편집 중인지 확신할 수 없다. 추가형 변경은 진행 중인 작업과 공존하지만, 침습형 변경은 충돌한다.

## 5. 재발명보다 재사용

**만들기 전에 grep하라. 이미 있는 것에 맞춰라.**

- 어떤 유틸리티, 헬퍼, 타입, 상수든 작성하기 전에 검색하라. 거의 비슷하지만 딱 맞지는 않는 것이 있다면, 새로 만들지 말고 확장하라.
- 처음부터 작성한다면 다르게 짰을 코드라도, 주변 코드의 스타일에 맞춰라. 이 저장소에서 통하는 방식이 일반적인 모범 답안보다 우선한다.
- 코드베이스 안에서 컨벤션이 실제로 갈라져 있다면 (세 파일은 A 방식, 세 파일은 B 방식), 불일치를 드러내고 사용자가 결정하게 하라. 제3의 스타일을 만들지 마라.

이유: 같은 일을 하는 두 구현이 가장 흔한 형태의 난장판이고, 30초의 grep이 나중에 들 30분짜리 청소를 막는다. 스타일 표류는 코드베이스 전반에서 가장 눈에 띄는 "지저분함"의 증상이고 나중에 고치기 가장 어렵다.

## 6. 외과적 변경

**필요한 것만 건드려라. 자신이 만든 것만 정리하라.**

- 본래 손대려던 게 아닌 인접한 코드, 주석, 포맷을 "개선"하지 마라. 그런 변경은 diff를 넓히고, 그 부분을 리팩토링하러 *온* 다른 에이전트와 충돌할 위험을 만들고, 실제 변경을 부수적인 잡음 뒤로 숨긴다.
- 범위 밖의 실제 버그나 냄새를 발견하면, 조용히 고치지 말고 메모하라 (대화에, TODO로, 이슈로).
- *당신의* 변경 때문에 사용되지 않게 된 임포트/변수/함수는 제거하라. 이미 있던 죽은 코드는 요청받지 않은 한 제거하지 마라.
- 스스로 물어보라: 시니어 엔지니어가 이게 과도하게 복잡하다고 말할까? 그렇다면 단순화하라.
- 제출 전에, 한 가지 질문을 던지며 전체 diff를 다시 읽어라: *낯선 사람의 눈으로 보았을 때, 이게 한 저자의 일관된 변경으로 보이는가, 아니면 기능 하나에 무관한 잔손질 셋이 더해진 것으로 보이는가?* 낯선 사람 시점 테스트를 통과하지 못하는 부분은 빼거나 명시적으로 설명하라.

이유: 변경된 모든 줄은 사용자 요청까지 곧장 거슬러 올라갈 수 있어야 한다. 범위가 부풀어 오르면 리뷰가 어려워지고, 다른 에이전트와의 충돌 위험이 커지고, 미래 기여자가 추론해야 할 표면이 늘어난다.

## 7. 다른 사람이 발견할 수 있는 인수인계를 작성하라

**다른 기여자들은 이 대화가 아니라 `git log`를 통해 당신의 결정을 발견한다.**

커밋 메시지와 PR 설명은 다음을 명시해야 한다:

- **범위 (Scope)**: 무엇이 변경되었고, 무엇은 의도적으로 건드리지 않았는지.
- **새로운 계약 (New contracts)**: 도입된 모든 공개 인터페이스, 타입, 스키마, 컨벤션. 다른 사람들이 grep으로 찾게 될 것이 바로 이 부분이다.
- **후속 작업 (Follow-ups)**: 하지 않았지만 누군가 해야 할 것들.

간결하게 — 에세이가 아니라 몇 줄. 대상 독자는 여섯 커밋 뒤의 또 다른 기여자 — 자신이 막 하려는 작업에 당신의 변경이 영향을 주는지 가늠하려는 사람이다.

## 8. 작업 규모에 따라 격식을 조정하라

**올바른 프로세스의 양은 코드베이스가 지저분해지지 않게 막는 최소한이다.**

과한 프로세스와 부족한 프로세스 모두 똑같이 지저분한 결과를 낳는다. §1~§7은 규율을 설명하고, 이 섹션은 언제 그 규율을 완화할지 말한다.

**사소하지 않은 작업** = 두어 개 이상의 파일을 건드리거나, "컴파일된다"보다 강한 검증 가능한 성공 기준이 있거나, 팀원에게 설명하고 싶어질 만한 변경. §1~§7은 사소하지 않은 작업에 전부 적용된다.

언제 완화할 것인가:

- **사소한 수정** (오타, 한 줄 버그, 주석 수정): §3의 계획 파일과 §7의 인수인계 형식을 건너뛰어도 된다. 명확한 커밋 메시지만 있으면 충분하다.
- **병렬 작업이 없는 단독 개발자**: §2의 병렬 브랜치 확인과 §4의 조율 절차는 대부분 오버헤드다. 컨벤션, 범위, 재사용 규칙은 여전히 적용된다 — 미래의 당신이 곧 팀원이다.
- **스파이크나 프로토타입**: 명확히 표시하라 (파일 헤더, 브랜치 이름, PR 제목). §5의 재사용과 §6의 단순화는 스파이크 안에서는 덜 중요하지만, 스파이크의 *경계*는 분명해야 한다 — 프로덕션처럼 재사용되지 않도록.

확인: 거대한 새 문서나 파일, 프로세스를 만들고 싶어진다면, 새 파일 맨 위에 한 단락을 적는 것으로 같은 일이 가능한지 먼저 물어라. 보통은 가능하다.

---

**이 가이드라인이 잘 작동하고 있다는 신호:** diff에 불필요한 변경이 줄고, 과한 복잡성 때문에 다시 짜는 일이 줄고, 명확화 질문이 실수 이후가 아니라 구현 이전에 나오기 시작한다.
